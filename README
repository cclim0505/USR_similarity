# HOW TO USE

Configure session_in.dat:

	Specify reference structure, one(1) only

	Specify target structure, can be multiple inputs and the SAME ATOMIC SIZE as reference structure

	Specify name of output file

    Specify the job type: 
    1) similarity calculation wrt a reference structure
    2) find unique structures from a set of given structures

    Specify the number of maximum unique structures programme wants to calculate

Run USR.out
